# Backend API Environment Example

# Server binding
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# MongoDB connection string (Database container is on port 5001)
# Example uses 'portfolio' as database name; adjust as needed.
MONGODB_URL=mongodb://localhost:5001/portfolio

# JWT secret for signing tokens (set to a strong secret in real env)
JWT_SECRET=changeme-in-prod

# CORS: Frontend origin for browser requests
FRONTEND_ORIGIN=http://localhost:3000

# Optional email notifications for contact form (stubbed in code)
# EMAIL_ENABLED=true
# CONTACT_INBOX=you@example.com
